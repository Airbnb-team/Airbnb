<!doctype html>
<html lang="ja">
  <head>
    <title>Airbnb</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <style>
      .dropzone {
        border: 2px dashed #b4bcc2;
        @include border-radius(4px);
      }
    </style>
  </head>

  <body>
    <%= render partial: 'shared/host_header' %>
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="space-content">
          <div class="center-content clearfix">
            <div class="main-panel-content">
              <div class="photo-title">お部屋の写真を更新しましょう</div>
                <div class="photo-card-content">

                  <script type="text/javascript">
                    Dropzone.options.myDropzone = {
                      parallelUploads: 1,
                      maxFiles: 5,
                    };
                  </script>

                  <%= form_for [@room, @photo], html:{multipart: true, class:"dropzone", id:"my-dropzone" } do |f| %>
                    <%= f.hidden_field :room_id, value: @room.id %>
                    <div class="dz-message needsclick">
                      <div class="edit-room-upload-btn">
                        <i class="fa fa-cloud-upload" aria-hidden="true"></i>
                        <span>写真をアップロード</span>
                      </div>
                      <div class="h5 text-normal">
                        <span>あるいは画像をドラッグ</span>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      <div class="main-panel-action-wrapper">
        <div class="container room-edit-bottom-space">
          <%= link_to '← 戻る', landing_rooms_path(current_user), class: 'h5 rooms-back-font' %>
          <%= f.submit '更新', class: 'rooms-update-btn' %>
        </div>
      </div>
    <% end %>
  </body>

</html>
